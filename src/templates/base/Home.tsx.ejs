import React from 'react';
import { Link } from 'react-router-dom';
import { Rocket, Shield, Layout, Palette, ArrowRight, Zap, Globe, Github } from 'lucide-react';

const Home: React.FC = () => {
  return (
    <div className="min-h-screen bg-white">
      {/* Navigation */}
      <nav className="fixed top-0 w-full z-50 bg-white/80 backdrop-blur-md border-b border-gray-100 px-6 py-4">
        <div className="max-w-7xl mx-auto flex justify-between items-center">
          <div className="flex items-center gap-2.5 group cursor-pointer">
            <div className="bg-brand-600 p-2 rounded-xl group-hover:rotate-6 transition-transform duration-300">
              <Rocket className="w-6 h-6 text-white" />
            </div>
            <span className="font-bold text-2xl text-gray-900 tracking-tight tracking-[-0.03em]"><%= projectName %></span>
          </div>
          
          <div className="hidden md:flex items-center gap-8">
            <a href="#features" className="text-sm font-semibold text-gray-600 hover:text-brand-600 transition-colors">Features</a>
            <a href="#" className="text-sm font-semibold text-gray-600 hover:text-brand-600 transition-colors">Resources</a>
            <a href="#" className="text-sm font-semibold text-gray-600 hover:text-brand-600 transition-colors">Pricing</a>
          </div>

          <div className="flex items-center gap-4">
            <Link 
              to="/login" 
              className="px-5 py-2.5 text-sm font-bold text-gray-700 hover:text-brand-600 transition-colors"
            >
              Sign In
            </Link>
            <Link 
              to="/dashboard" 
              className="px-6 py-2.5 bg-gray-900 text-white text-sm font-bold rounded-full hover:bg-gray-800 transition-all shadow-md active:scale-95"
            >
              Get Started
            </Link>
          </div>
        </div>
      </nav>

      <main className="pt-24">
        {/* Hero Section */}
        <section className="relative px-6 pt-20 pb-32 overflow-hidden">
          <div className="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full -z-10 opacity-50">
             <div className="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] rounded-full bg-brand-100/50 blur-[120px]" />
             <div className="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] rounded-full bg-brand-50/50 blur-[120px]" />
          </div>

          <div className="max-w-7xl mx-auto text-center">
            <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-brand-50 border border-brand-100 text-brand-700 text-xs font-bold mb-8 animate-fade-in">
              <span className="flex h-2 w-2 rounded-full bg-brand-500 animate-pulse" />
              v1.0.0 Now Available
            </div>
            
            <h1 className="text-6xl md:text-8xl font-black text-gray-900 mb-8 tracking-tighter leading-[0.9]">
              Build your <span className="text-brand-600">next big idea</span> faster than ever.
            </h1>
            
            <p className="text-xl md:text-2xl text-gray-500 max-w-3xl mx-auto mb-12 font-medium leading-relaxed">
              The ultimate React boilerplate with enterprise-grade features pre-configured. 
              Zero setup, maximum performance.
            </p>

            <div className="flex flex-col sm:flex-row justify-center items-center gap-6">
              <Link 
                to="/dashboard" 
                className="group px-10 py-5 bg-brand-600 text-white font-bold rounded-2xl hover:bg-brand-700 transition-all shadow-2xl shadow-brand-100 flex items-center gap-3 active:scale-95"
              >
                Launch Dashboard
                <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
              </Link>
              <a 
                href="#" 
                className="px-10 py-5 bg-white text-gray-900 border-2 border-gray-100 font-bold rounded-2xl hover:bg-gray-50 transition-all flex items-center gap-3 active:scale-95"
              >
                <Github className="w-5 h-5" />
                Star on GitHub
              </a>
            </div>

            <div className="mt-20 pt-10 border-t border-gray-50">
              <p className="text-sm font-bold text-gray-400 uppercase tracking-widest mb-10">Trusted by modern engineering teams</p>
              <div className="flex flex-wrap justify-center gap-12 grayscale opacity-40">
                <LogoPlaceholder name="Vercel" />
                <LogoPlaceholder name="Stripe" />
                <LogoPlaceholder name="Discord" />
                <LogoPlaceholder name="Linear" />
                <LogoPlaceholder name="Loom" />
              </div>
            </div>
          </div>
        </section>

        {/* Features Grid */}
        <section id="features" className="py-32 bg-gray-50/50">
          <div className="max-w-7xl mx-auto px-6">
            <div className="text-center mb-20">
              <h2 className="text-4xl md:text-5xl font-black text-gray-900 mb-6 tracking-tight">Everything you need, <br/>nothing you don't.</h2>
              <p className="text-gray-500 text-lg font-medium">Carefully selected toolstack for modern web development.</p>
            </div>

            <div className="grid md:grid-cols-3 gap-8">
              <FeatureCard 
                icon={<Zap className="w-8 h-8 text-brand-600" />}
                title="Lightning Fast"
                description="Built on Vite for sub-second HMR and optimized production builds that score 100 on Lighthouse."
              />
              <FeatureCard 
                icon={<Shield className="w-8 h-8 text-brand-600" />}
                title="Secure by Default"
                description="Pre-configured authentication context with JWT support and protected route middleware."
              />
              <FeatureCard 
                icon={<Globe className="w-8 h-8 text-brand-600" />}
                title="Ready to Scale"
                description="Modular architecture that grows with your team, from simple prototypes to complex platforms."
              />
            </div>
          </div>
        </section>

        {/* CTA Section */}
        <section className="px-6 py-32">
          <div className="max-w-5xl mx-auto bg-brand-600 rounded-[3rem] p-12 md:p-20 text-center relative overflow-hidden shadow-3xl shadow-brand-200">
            <div className="absolute top-0 right-0 w-64 h-64 bg-brand-500 rounded-full -mr-32 -mt-32 opacity-50" />
            <div className="absolute bottom-0 left-0 w-64 h-64 bg-brand-700 rounded-full -ml-32 -mb-32 opacity-50" />
            
            <div className="relative z-10">
              <h2 className="text-4xl md:text-6xl font-black text-white mb-8 tracking-tight">Ready to start building?</h2>
              <p className="text-brand-100 text-xl font-medium mb-12 max-w-2xl mx-auto">Join 1,000+ developers building premium applications with <%= projectName %>.</p>
              <Link 
                to="/dashboard" 
                className="inline-flex px-12 py-6 bg-white text-brand-600 font-black rounded-2xl hover:bg-gray-50 transition-all shadow-xl hover:shadow-2xl transform hover:-translate-y-1 active:scale-95"
              >
                Create Account Now
              </Link>
            </div>
          </div>
        </section>
      </main>

      <footer className="border-t border-gray-100 py-20 px-6">
        <div className="max-w-7xl mx-auto">
          <div className="grid md:grid-cols-4 gap-12 mb-16">
            <div className="col-span-2">
              <div className="flex items-center gap-2.5 mb-6">
                <div className="bg-brand-600 p-1.5 rounded-lg">
                  <Rocket className="w-5 h-5 text-white" />
                </div>
                <span className="font-bold text-xl text-gray-900 tracking-tight"><%= projectName %></span>
              </div>
              <p className="text-gray-500 font-medium max-w-sm">Making high-quality software development accessible to everyone, everywhere.</p>
            </div>
            <div>
              <h4 className="font-bold text-gray-900 mb-6 uppercase text-xs tracking-widest">Product</h4>
              <ul className="space-y-4 text-sm font-semibold text-gray-500">
                <li><a href="#" className="hover:text-brand-600 transition-colors">Features</a></li>
                <li><a href="#" className="hover:text-brand-600 transition-colors">Templates</a></li>
                <li><a href="#" className="hover:text-brand-600 transition-colors">Integrations</a></li>
              </ul>
            </div>
            <div>
              <h4 className="font-bold text-gray-900 mb-6 uppercase text-xs tracking-widest">Company</h4>
              <ul className="space-y-4 text-sm font-semibold text-gray-500">
                <li><a href="#" className="hover:text-brand-600 transition-colors">About Us</a></li>
                <li><a href="#" className="hover:text-brand-600 transition-colors">Privacy Policy</a></li>
                <li><a href="#" className="hover:text-brand-600 transition-colors">Terms of Service</a></li>
              </ul>
            </div>
          </div>
          <div className="pt-8 border-t border-gray-50 flex flex-col md:flex-row justify-between items-center gap-6">
            <p className="text-gray-400 text-sm font-bold tracking-tight">
              © 2024 <%= projectName %>. All rights reserved.
            </p>
            <div className="flex gap-6">
              <SocialIcon icon={<Github className="w-5 h-5" />} />
              <SocialIcon icon={<Globe className="w-5 h-5" />} />
            </div>
          </div>
        </div>
      </footer>
    </div>
  );
};

const FeatureCard: React.FC<{ icon: React.ReactNode, title: string, description: string }> = ({ icon, title, description }) => (
  <div className="p-10 bg-white border border-gray-100 rounded-[2.5rem] hover:shadow-2xl hover:border-brand-100 transition-all duration-500 group">
    <div className="bg-brand-50 w-16 h-16 rounded-2xl flex items-center justify-center mb-8 group-hover:scale-110 group-hover:rotate-3 transition-transform duration-500">
      {icon}
    </div>
    <h3 className="text-2xl font-black text-gray-900 mb-4 tracking-tight">{title}</h3>
    <p className="text-gray-500 font-medium leading-relaxed">{description}</p>
  </div>
);

const LogoPlaceholder: React.FC<{ name: string }> = ({ name }) => (
  <div className="font-black text-2xl tracking-tighter text-gray-900">{name}</div>
);

const SocialIcon: React.FC<{ icon: React.ReactNode }> = ({ icon }) => (
  <a href="#" className="text-gray-400 hover:text-brand-600 transition-colors duration-300">
    {icon}
  </a>
);

export default Home;
